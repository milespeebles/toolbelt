import React from 'react'
import 'sanitize.css'
import 'sanitize.css/forms.css'
import 'sanitize.css/typography.css'

import './site.css'

const Site =
  ({ children }) => (
    <div>
      {children}
    </div>
  )

export default Site
